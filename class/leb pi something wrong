def leb(terms):
    acc=0.0
    fenzi=4.0
    fenmu=1.0


    for turnterm in range(terms):

        nextterm=(fenzi*1.0)/(fenmu*(-1.0)**turnterm)*1.0
        acc=acc+nextterm
        fenmu=fenmu+2.0

        return acc

print leb(100)
